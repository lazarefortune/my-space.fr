<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">My Space</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor03">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{ menu_active('app_home') }}" href="{{ path('app_home') }}">
                        {{ icon('home', 'lg') }}
                        {{ 'Home'|trans }}
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ menu_active('app_story_index') }}" href="{{ path('app_story_index') }}">
                        {{ icon('book', 'lg') }}
                        {{ 'Stories'|trans }}
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ menu_active('app_about') }}" href="{{ path('app_about') }}">
                        {{ icon('info-circle', 'lg') }}
                        {{ 'About'|trans }}
                    </a>
                </li>
            </ul>

            {% if app.user %}
                <ul class="navbar-nav">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                            {{ app.user.fullname }}
                        </a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path('app_dashboard') }}">Mon espace</a>
                            <a class="dropdown-item" href="{{ path('app_profile') }}">Mon compte</a>
                            <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
                        </div>
                    </li>
                </ul>
            {% else %}
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link {{ menu_active('app_login') }}" href="{{ path('app_login') }}">
                            {{ icon('sign-in-alt', 'lg') }}
                            {{ 'Login'|trans }}
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ menu_active('app_register') }}" href="{{ path('app_register') }}">
                            {{ icon('user-plus', 'lg') }}
                            {{ 'Register'|trans }}
                        </a>
                    </li>
                </ul>
            {% endif %}

{#            change language#}
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                        {{ icon('globe', 'lg') }}
                        {{ app.request.locale|upper }}
                    </a>
                    <div class="dropdown-menu">
                        {% for locale in locales %}
                            {% if locale != app.request.locale %}
                                <a class="dropdown-item" href="{{ path('app_change_language', {'locale': locale}) }}">
                                    {{ locale|upper }}
                                </a>
                            {% endif %}
                        {% endfor %}
                    </div>
                </li>
            </ul>

            <form class="d-flex">
                <label>
                    <input class="form-control me-sm-2" type="search" placeholder=" {{ 'A question'|trans }} ?">
                </label>
                <button class="btn btn-secondary my-2 my-sm-0" type="submit">{{ 'Search'|trans }}</button>
            </form>
        </div>
    </div>
</nav>