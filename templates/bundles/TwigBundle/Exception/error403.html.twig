{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-12 d-flex justify-content-center align-items-center mt-5">
                <div class="error-template text-center">
                    <h1>
                        Oops!
                    </h1>
                    <h2 class="display-1 mb-2">
                        403 {{ 'error.403.title'|trans }}
                    </h2>
                    <div class="error-details mb-4">
                        {{ 'error.403.description'|trans }}
                    </div>
                    <div class="error-actions">
                        <a href="{{ path('app_home') }}" class="btn btn-primary btn-lg"><span
                                    class="glyphicon glyphicon-home"></span>
                            {{ 'error.403.btn-label'|trans }}
                        </a>
                        <a href="{{ path('app_contact') }}" class="btn btn-default btn-lg"><span
                                    class="glyphicon glyphicon-envelope"></span> {{ 'error.403.btn-support-label'|trans }} </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}